import { FormikProps, FormikValues } from "formik";
import React from "react";

const SecondSection: React.FC<FormikProps<FormikValues>> = (props) => {
  return <div />;
};

export default SecondSection;
